# Epic Tech AI Cinematic Studio

**Production-ready cinematic AI studio** with immersive 3D scene, multi-agent workflow, FLUX.1 generation, Memory Vault (Vectorize), R2 storage, and luxury dark-neon cyberpunk UI.

All files created. Ready for `npm install`, `npm run dev`, and `npm run cf:deploy`.

**Next step**: Run `npm install` and test the studio.